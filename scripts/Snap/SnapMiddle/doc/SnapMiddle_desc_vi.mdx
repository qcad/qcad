import SnapMiddleFigure from './SnapMiddleFigure.png';

Bắt vào các điểm giữa của các đường thẳng và cung tròn và vào các đối tượng điểm. Lưu ý rằng điểm giữa của một cung tròn nằm ở giữa đường cung trên cung tròn, không phải ở tâm của nó.

<Image src={SnapMiddleFigure} alt="SnapMiddleFigure" width={182} height={60} />
