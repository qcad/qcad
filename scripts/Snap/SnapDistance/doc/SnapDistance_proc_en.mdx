import SnapDistanceFigure from './SnapDistanceFigure.png';

1. Enter the distance into the options toolbar.
2. Move the cursor to the entity along which the distance should be
 measured.
3. When the desired position / preview is shown, click the left mouse
 button to set the coordinate.

<Image src={SnapDistanceFigure} alt="SnapDistanceFigure" width={60} height={30} />
