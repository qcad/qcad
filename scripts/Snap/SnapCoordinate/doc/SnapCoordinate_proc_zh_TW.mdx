import Tick from './Tick.png';

1. 當您必須指定一個點並想通過輸入一對座標 (x/y) 來做到這一點時，啟動此工具。
2. 在選項工具列中輸入座標，並選擇它們是絕對座標還是相對座標（相對於相對零點）。
3. 按一下 OK 按鈕或按 Enter 確認輸入並設定座標：  
   <Image src={Tick} alt="Tick" width={40} height={40} />
