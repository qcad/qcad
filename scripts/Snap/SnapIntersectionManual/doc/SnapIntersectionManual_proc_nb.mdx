import SnapIntersectionManualFigure from './SnapIntersectionManualFigure.png';

1. Etter at du har aktivert dette festeverktøyet, klikk på det første av de to
   skjærende objektene.
2. Klikk på det andre av de to skjærende objektene. Hvis to skjæringspunkter
   er mulige, sørg for å klikke på det andre objektet et sted nærmere
   skjæringspunktet du vil feste til.

   <Image src={SnapIntersectionManualFigure} alt="SnapIntersectionManualFigure" width={77} height={94} />

