import Tick from './Tick.png';

1. 当您必须指定一个点并想通过输入极坐标来做到这一点时，启动此工具。
2. 在选项工具栏中输入极坐标（半径和角度），并选择它是绝对坐标还是相对坐标（相对于相对零点）。
3. 单击 OK 按钮或按 Enter 确认输入并设置坐标：  
   <Image src={Tick} alt="Tick" width={40} height={40} />
