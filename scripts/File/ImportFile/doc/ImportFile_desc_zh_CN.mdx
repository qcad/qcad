导入命令将磁盘上文件中的图形插入到当前图形中。  
导入图形使用的所有图层都将添加到当前图形的图层列表中。如果需要，可以覆盖同名的现有图层（选项工具栏中的"overwrite layers"选项）。  
导入图形的块参照将与它们引用的块定义一起插入。如果需要，可以覆盖当前图形中的块（选项工具栏中的"overwrite blocks"选项）。  
选项工具栏还提供了一些工具，用于在定位导入图形时缩放、旋转或翻转它。
