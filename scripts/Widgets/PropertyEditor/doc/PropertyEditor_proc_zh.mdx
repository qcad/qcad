1. 使用选择工具选择要查看或更改属性的一个或多个实体。
2. 所选内容的属性会显示在属性编辑器中。

如果选择了单个实体，则会显示其所有属性，包括其几何图形（例如直线的起点和终点，或圆的圆心和半径）。如果选择了多个实体，属性编辑器将显示所有所选实体共有的属性。在所选内容中具有不同值的属性会显示为混合字段。

要更改属性，请在属性编辑器的相应字段中输入新值并确认输入（Enter 或 Tab）。

更改会立即应用于所有所选实体。通过这种方式，可以编辑几何属性、图层、颜色、线宽和线型等属性，以及自定义属性。

如果未选择任何内容，属性编辑器为空。
