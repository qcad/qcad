Lệnh này hiển thị hộp thoại lớp cho lớp hiện tại. Đây là cùng một hộp thoại cũng được dùng để tạo các lớp mới. Dùng nó để thay đổi tên lớp hoặc các thuộc tính của nó.  
Tên của lớp „0" không bao giờ có thể thay đổi.
