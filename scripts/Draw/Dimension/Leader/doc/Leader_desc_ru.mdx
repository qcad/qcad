import LeaderFigure from './LeaderFigure.png';

Выноски — это стрелки, которые обычно указывают от текстового объекта к другому
объекту, как показано ниже. В примере текстовый объект «N7» описывает свойство
поверхности, указывая на неё выноской.  

<Image src={LeaderFigure} alt="LeaderFigure" width={199} height={111} />

