import LineFreehandFigure from './LineFreehandFigure.png';

使用此工具，您可以绘制手绘线。由于技术图纸需要绝对精确，因此此工具通常很少使用。但是，在某些情况下，此工具可能会派上用场，例如用于如下所示的断裂边缘：  

<Image src={LineFreehandFigure} alt="LineFreehandFigure" width={356} height={214} />
