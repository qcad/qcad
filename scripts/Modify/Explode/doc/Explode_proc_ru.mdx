1. Выберите объекты, которые хотите разбить.
2. Запустите этот инструмент, чтобы разбить выбранные объекты.
3. Повторите шаг 2, чтобы при необходимости далее разбить выбранные объекты.
   Если объект нельзя разбить далее, он остаётся без изменений.
