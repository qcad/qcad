import RoundFigure from './RoundFigure.png';

1. Введіть радіус заокруглення на панелі параметрів (наприклад 5).
2. Установіть прапорець „Trim", якщо ви хочете автоматично обрізати об'єкти. Якщо параметр вимкнено, два крайові об'єкти залишаться незмінними.
3. Виберіть перший крайовий об'єкт (лінію або дугу).
4. Виберіть другий об'єкт.<br/>

   <Image src={RoundFigure} alt="RoundFigure" width={270} height={139} />
