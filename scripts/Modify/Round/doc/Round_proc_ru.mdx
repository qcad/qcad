import RoundFigure from './RoundFigure.png';

1. Введите радиус скругления на панели опций (например, 5).
2. Установите флажок «Обрезать», если хотите обрезать объекты автоматически. Если опция отключена, два угловых объекта останутся нетронутыми.
3. Выберите первый краевой объект (линию или дугу).
4. Выберите второй объект.<br/>

   <Image src={RoundFigure} alt="RoundFigure" width={270} height={139} />

