import RoundFigure from './RoundFigure.png';

1. Nhập bán kính của phần bo tròn trong thanh công cụ tùy chọn (ví dụ 5).
2. Đánh dấu „Trim" nếu bạn muốn cắt tỉa các đối tượng tự động. Nếu tùy chọn bị tắt, hai đối tượng góc sẽ vẫn không bị chạm đến.
3. Chọn đối tượng cạnh thứ nhất (một đường thẳng hoặc cung tròn).
4. Chọn đối tượng thứ hai.<br/>

   <Image src={RoundFigure} alt="RoundFigure" width={270} height={139} />
