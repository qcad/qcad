Command-line tools are standalone scripts run from a terminal instead of
from the QCAD graphical interface. They use the same drawing engine as
QCAD to process drawing files in batch, without opening a window, which
makes them well suited for automation.
